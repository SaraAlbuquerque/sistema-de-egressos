diff --git a/pom.xml b/pom.xml
index 1111111..2222222 100644
--- a/pom.xml
+++ b/pom.xml
@@ -40,6 +40,13 @@
       <artifactId>commons-csv</artifactId>
       <version>1.10.0</version>
     </dependency>
+    <!-- PDF export -->
+    <dependency>
+      <groupId>com.github.librepdf</groupId>
+      <artifactId>openpdf</artifactId>
+      <version>1.3.32</version>
+    </dependency>
   </dependencies>
 </project>
diff --git a/src/main/java/com/egressos/model/TipoEvento.java b/src/main/java/com/egressos/model/TipoEvento.java
new file mode 100644
index 0000000..aaaaaaaa
--- /dev/null
+++ b/src/main/java/com/egressos/model/TipoEvento.java
@@ -0,0 +1,20 @@
+package com.egressos.model;
+
+public enum TipoEvento {
+    PROMOCAO,
+    PROJETO,
+    PREMIO,
+    PUBLICACAO,
+    OUTRO;
+
+    public static TipoEvento fromString(String s) {
+        if (s == null || s.isBlank()) return OUTRO;
+        String k = s.trim().toUpperCase();
+        for (TipoEvento t : values()) {
+            if (t.name().equals(k)) return t;
+        }
+        // tolera valores antigos gravados em CSV:
+        return OUTRO;
+    }
+}
diff --git a/src/main/java/com/egressos/model/EventoChave.java b/src/main/java/com/egressos/model/EventoChave.java
index bbbbbbb..ccccccc 100644
--- a/src/main/java/com/egressos/model/EventoChave.java
+++ b/src/main/java/com/egressos/model/EventoChave.java
@@ -1,18 +1,29 @@
 package com.egressos.model;
 
 import java.time.LocalDate;
 
 public class EventoChave {
     private String id;
     private String egressoId;
-    private String tipo; // texto livre (antigo)
+    private TipoEvento tipo; // agora enum padronizado
     private String titulo;
     private String organizacao;
     private String local;
     private String descricao;
     private LocalDate data;
 
     public EventoChave(){}
 
     // getters/setters
     public String getId() { return id; }
     public void setId(String id) { this.id = id; }
     public String getEgressoId() { return egressoId; }
     public void setEgressoId(String egressoId) { this.egressoId = egressoId; }
-    public String getTipo() { return tipo; }
-    public void setTipo(String tipo) { this.tipo = tipo; }
+    public TipoEvento getTipo() { return tipo; }
+    public void setTipo(TipoEvento tipo) { this.tipo = tipo; }
+    // retrocompat: para código antigo que chamava setTipo(String)
+    public void setTipo(String tipoTexto) { this.tipo = TipoEvento.fromString(tipoTexto); }
     public String getTitulo() { return titulo; }
     public void setTitulo(String titulo) { this.titulo = titulo; }
     public String getOrganizacao() { return organizacao; }
@@ -26,3 +37,4 @@ public class EventoChave {
     public LocalDate getData() { return data; }
     public void setData(LocalDate data) { this.data = data; }
 }
diff --git a/src/main/java/com/egressos/dao/EventosDao.java b/src/main/java/com/egressos/dao/EventosDao.java
index ddddddd..eeeeeee 100644
--- a/src/main/java/com/egressos/dao/EventosDao.java
+++ b/src/main/java/com/egressos/dao/EventosDao.java
@@ -1,6 +1,7 @@
 package com.egressos.dao;
 
 import com.egressos.model.EventoChave;
+import com.egressos.model.TipoEvento;
 import java.time.LocalDate;
 import java.util.*;
 
@@ -30,10 +31,10 @@ public class EventosDao {
         // cabeçalho CSV esperado:
         // id,egressoId,tipo,titulo,organizacao,local,descricao,data(YYYY-MM-DD)
         for (String[] row : csv.readAll("eventos.csv")) {
             EventoChave e = new EventoChave();
             e.setId(row[0]);
             e.setEgressoId(row[1]);
-            e.setTipo(row[2]); // era texto, agora converte
+            e.setTipo(TipoEvento.fromString(row[2]));
             e.setTitulo(row[3]);
             e.setOrganizacao(row[4]);
             e.setLocal(row[5]);
             e.setDescricao(row[6]);
             e.setData(row[7]==null||row[7].isBlank()? null : LocalDate.parse(row[7]));
@@ -50,7 +51,7 @@ public class EventosDao {
             out.add(new String[]{
                 e.getId(),
                 e.getEgressoId(),
-                e.getTipo()==null? "" : e.getTipo(),
+                e.getTipo()==null? "" : e.getTipo().name(),
                 n(e.getTitulo()),
                 n(e.getOrganizacao()),
                 n(e.getLocal()),
                 n(e.getDescricao()),
                 e.getData()==null? "" : e.getData().toString()
diff --git a/src/main/java/com/egressos/model/EgressoProfile.java b/src/main/java/com/egressos/model/EgressoProfile.java
index ffffff1..ffffff2 100644
--- a/src/main/java/com/egressos/model/EgressoProfile.java
+++ b/src/main/java/com/egressos/model/EgressoProfile.java
@@ -15,6 +15,10 @@ public class EgressoProfile {
     private String estado;
     private String pais;
     private boolean empregado;
     private String empresaAtual;
+    // LGPD / visibilidade
+    private boolean permitirExibirContato;
+    private boolean permitirExibirEmpresa;
+
     // getters/setters existentes ...
+
+    public boolean isPermitirExibirContato(){ return permitirExibirContato; }
+    public void setPermitirExibirContato(boolean v){ this.permitirExibirContato = v; }
+    public boolean isPermitirExibirEmpresa(){ return permitirExibirEmpresa; }
+    public void setPermitirExibirEmpresa(boolean v){ this.permitirExibirEmpresa = v; }
 }
diff --git a/src/main/java/com/egressos/dao/EgressosDao.java b/src/main/java/com/egressos/dao/EgressosDao.java
index 1234567..7654321 100644
--- a/src/main/java/com/egressos/dao/EgressosDao.java
+++ b/src/main/java/com/egressos/dao/EgressosDao.java
@@ -1,5 +1,6 @@
 package com.egressos.dao;
 import com.egressos.model.EgressoProfile;
+import java.util.Arrays;
 import java.util.ArrayList;
 import java.util.List;
 
@@ -12,9 +13,12 @@ public class EgressosDao {
     public List<EgressoProfile> listarTodos(){
         List<EgressoProfile> out = new ArrayList<>();
-        // header antigo: usuarioId,curso,ano,area,escolaridade,empregado,empresa,cidade,estado,pais
+        // header novo (acrescenta LGPD no fim):
+        // usuarioId,curso,ano,area,escolaridade,empregado,empresa,cidade,estado,pais,permitirExibirContato,permitirExibirEmpresa
         for (String[] row : csv.readAll("egressos.csv")) {
             EgressoProfile p = new EgressoProfile();
             p.setUsuarioId(row[0]);
             p.setCurso(row[1]);
             p.setAnoFormacao(Integer.parseInt(row[2]));
             p.setAreaAtuacao(row[3]);
             p.setEscolaridadeAtual(row[4]);
             p.setEmpregado(Boolean.parseBoolean(row[5]));
             p.setEmpresaAtual(row[6]);
             p.setCidade(row[7]);
             p.setEstado(row[8]);
             p.setPais(row[9]);
+            // retrocompatibilidade: se não vier as colunas extras, assume false
+            if (row.length > 11) {
+                p.setPermitirExibirContato(Boolean.parseBoolean(row[10]));
+                p.setPermitirExibirEmpresa(Boolean.parseBoolean(row[11]));
+            }
             out.add(p);
         }
         return out;
@@ -28,10 +32,13 @@ public class EgressosDao {
         List<String[]> out = new ArrayList<>();
         for (EgressoProfile p : all) {
             out.add(new String[]{
                 p.getUsuarioId(),
                 n(p.getCurso()),
                 String.valueOf(p.getAnoFormacao()),
                 n(p.getAreaAtuacao()),
                 n(p.getEscolaridadeAtual()),
                 String.valueOf(p.isEmpregado()),
                 n(p.getEmpresaAtual()),
                 n(p.getCidade()), n(p.getEstado()), n(p.getPais()),
+                String.valueOf(p.isPermitirExibirContato()),
+                String.valueOf(p.isPermitirExibirEmpresa())
             });
         }
         csv.writeAll("egressos.csv", out, new String[]{
-            "usuarioId","curso","ano","area","escolaridade","empregado","empresa","cidade","estado","pais"
+            "usuarioId","curso","ano","area","escolaridade","empregado","empresa","cidade","estado","pais","permitirExibirContato","permitirExibirEmpresa"
         });
     }
 }
diff --git a/src/main/java/com/egressos/ui/EventosDialog.java b/src/main/java/com/egressos/ui/EventosDialog.java
index ababab0..ababab9 100644
--- a/src/main/java/com/egressos/ui/EventosDialog.java
+++ b/src/main/java/com/egressos/ui/EventosDialog.java
@@ -1,11 +1,14 @@
 package com.egressos.ui;
 
 import com.egressos.dao.EventosDao;
 import com.egressos.model.EventoChave;
+import com.egressos.model.TipoEvento;
 import javax.swing.*;
 import java.awt.*;
 
 public class EventosDialog extends JDialog {
-    private final JTextField tipoField = new JTextField();
+    private final JComboBox<TipoEvento> tipoCombo = new JComboBox<>(TipoEvento.values());
     // ... demais campos
@@ -30,7 +33,7 @@ public class EventosDialog extends JDialog {
-        form.add(new JLabel("Tipo:")); form.add(tipoField);
+        form.add(new JLabel("Tipo:")); form.add(tipoCombo);
         // ...
     }
 
     private EventoChave buildFromForm() {
         EventoChave e = new EventoChave();
-        e.setTipo(tipoField.getText().trim());
+        e.setTipo((TipoEvento) tipoCombo.getSelectedItem());
         // ... preencher demais
         return e;
     }
@@ -52,6 +55,14 @@ public class EventosDialog extends JDialog {
         table.setAutoCreateRowSorter(true);
         // garantir sort por data desc (coluna 6)
         var sorter = new javax.swing.table.TableRowSorter<>(model);
         table.setRowSorter(sorter);
         var ks = new java.util.ArrayList<javax.swing.RowSorter.SortKey>();
         ks.add(new javax.swing.RowSorter.SortKey(6, javax.swing.SortOrder.DESCENDING));
         sorter.setSortKeys(ks);
     }
diff --git a/src/main/java/com/egressos/ui/EgressoFrame.java b/src/main/java/com/egressos/ui/EgressoFrame.java
index 1212121..3434343 100644
--- a/src/main/java/com/egressos/ui/EgressoFrame.java
+++ b/src/main/java/com/egressos/ui/EgressoFrame.java
@@ -1,10 +1,17 @@
 package com.egressos.ui;
 
 import com.egressos.dao.EventosDao;
 import com.egressos.dao.EgressosDao;
 import com.egressos.model.EventoChave;
 import com.egressos.model.EgressoProfile;
 import com.egressos.model.Usuario;
+import javax.swing.table.DefaultTableModel;
 import javax.swing.*;
 import java.awt.*;
 import java.util.List;
+import java.util.Comparator;
+import java.util.stream.Collectors;
 
 public class EgressoFrame extends JFrame {
     // ... existentes
+    private final JCheckBox cbVisContato = new JCheckBox("Permitir exibir meu contato a docentes");
+    private final JCheckBox cbVisEmpresa = new JCheckBox("Permitir exibir minha empresa a docentes");
+    private final DefaultTableModel recentesModel = new DefaultTableModel(new Object[]{"Data","Tipo","Título"},0){ public boolean isCellEditable(int r,int c){return false;}};
 
     public EgressoFrame(Usuario egresso){
         super("Área do Egresso");
         setSize(1000,600);
         setLocationRelativeTo(null);
         setDefaultCloseOperation(EXIT_ON_CLOSE);
 
         JTabbedPane tabs = new JTabbedPane();
-        tabs.add("Perfil", buildPerfilTab());
+        tabs.add("Perfil", buildPerfilTab());
+        tabs.add("Recentes", buildRecentesTab(egresso));
         tabs.add("Eventos", new EventosDialog(this, egresso).getContentPane());
         setContentPane(tabs);
     }
 
     private JPanel buildPerfilTab(){
         JPanel p = new JPanel(new GridBagLayout());
         // ... campos já existentes do perfil
+        GridBagConstraints c = new GridBagConstraints();
+        c.insets = new Insets(4,4,4,4);
+        c.gridx=0; c.gridy=99; c.anchor=GridBagConstraints.WEST;
+        p.add(cbVisContato, c);
+        c.gridy=100;
+        p.add(cbVisEmpresa, c);
         return p;
     }
 
+    private JPanel buildRecentesTab(Usuario egresso){
+        JPanel p = new JPanel(new BorderLayout());
+        JTable t = new JTable(recentesModel);
+        t.setAutoCreateRowSorter(true);
+        p.add(new JScrollPane(t), BorderLayout.CENTER);
+        carregarRecentes(egresso);
+        return p;
+    }
+
+    private void carregarRecentes(Usuario egresso){
+        List<EventoChave> todos = new EventosDao().listarTodos()
+                .stream()
+                .filter(e -> egresso.getId().equals(e.getEgressoId()))
+                .sorted(Comparator.comparing(EventoChave::getData, Comparator.nullsLast(Comparator.naturalOrder())).reversed())
+                .limit(5)
+                .collect(Collectors.toList());
+        recentesModel.setRowCount(0);
+        for (EventoChave e: todos){
+            recentesModel.addRow(new Object[]{
+                    e.getData()==null? "" : e.getData().toString(),
+                    e.getTipo()==null? "" : e.getTipo().name(),
+                    e.getTitulo()
+            });
+        }
+    }
 }
diff --git a/src/main/java/com/egressos/dao/VisualizacoesDocenteDao.java b/src/main/java/com/egressos/dao/VisualizacoesDocenteDao.java
new file mode 100644
index 0000000..4444444 100644
--- /dev/null
+++ b/src/main/java/com/egressos/dao/VisualizacoesDocenteDao.java
@@ -0,0 +1,54 @@
+package com.egressos.dao;
+
+import java.time.LocalDateTime;
+import java.util.ArrayList;
+import java.util.List;
+import java.util.stream.Collectors;
+
+public class VisualizacoesDocenteDao {
+    private final CsvStore csv = CsvStore.get();
+
+    public static class Row {
+        public String docenteId;
+        public String egressoUsuarioId;
+        public LocalDateTime ts;
+    }
+
+    public void registrar(String docenteId, String egressoUsuarioId){
+        List<String[]> all = csv.readAll("visualizacoes_docente.csv");
+        all.add(new String[]{ docenteId, egressoUsuarioId, LocalDateTime.now().toString() });
+        csv.writeAll("visualizacoes_docente.csv", all, new String[]{"docenteId","egressoUsuarioId","ts"});
+    }
+
+    public List<Row> ultimos(String docenteId, int limit){
+        List<Row> rows = new ArrayList<>();
+        for (String[] r : csv.readAll("visualizacoes_docente.csv")){
+            if (r.length<3) continue;
+            if (!docenteId.equals(r[0])) continue;
+            Row row = new Row();
+            row.docenteId = r[0];
+            row.egressoUsuarioId = r[1];
+            row.ts = LocalDateTime.parse(r[2]);
+            rows.add(row);
+        }
+        return rows.stream()
+                .sorted((a,b) -> b.ts.compareTo(a.ts))
+                .limit(limit)
+                .collect(Collectors.toList());
+    }
+}
diff --git a/src/main/java/com/egressos/ui/DocenteFrame.java b/src/main/java/com/egressos/ui/DocenteFrame.java
index 9999999..8888888 100644
--- a/src/main/java/com/egressos/ui/DocenteFrame.java
+++ b/src/main/java/com/egressos/ui/DocenteFrame.java
@@ -1,12 +1,19 @@
 package com.egressos.ui;
 
 import com.egressos.dao.EgressosDao;
 import com.egressos.dao.EventosDao;
 import com.egressos.dao.UsuariosDao;
+import com.egressos.dao.VisualizacoesDocenteDao;
 import com.egressos.model.EgressoProfile;
 import com.egressos.model.EventoChave;
 import com.egressos.model.Usuario;
 
 import javax.swing.*;
 import javax.swing.table.DefaultTableModel;
 import java.awt.*;
 import java.time.format.DateTimeFormatter;
 import java.util.*;
 import java.util.List;
 import java.util.stream.Collectors;
 
 public class DocenteFrame extends JFrame {
@@
-    private final JTable table = new JTable(model);
+    private final JTable table = new JTable(model);
+    private final DefaultTableModel recentesModel = new DefaultTableModel(new Object[]{"Quando","Egresso"},0){ public boolean isCellEditable(int r,int c){return false;}};
+    private final VisualizacoesDocenteDao visDao = new VisualizacoesDocenteDao();
+    private final Usuario docenteRef;
@@
-    public DocenteFrame(Usuario docente) {
+    public DocenteFrame(Usuario docente) {
+        this.docenteRef = docente;
         super("Egressos — Consulta (Docente)");
         setDefaultCloseOperation(EXIT_ON_CLOSE);
         setSize(1000, 600);
         setLocationRelativeTo(null);
@@
-        add(new JScrollPane(table), BorderLayout.CENTER);
+        JSplitPane split = new JSplitPane(JSplitPane.HORIZONTAL_SPLIT);
+        split.setResizeWeight(0.8);
+        split.setLeftComponent(new JScrollPane(table));
+        split.setRightComponent(buildRecentesPanel());
+        add(split, BorderLayout.CENTER);
@@
-        buscarBtn.addActionListener(e -> aplicarBusca());
+        buscarBtn.addActionListener(e -> aplicarBusca());
+        JButton verBtn = new JButton("Registrar visualização do selecionado");
+        verBtn.addActionListener(e -> registrarVisualizacaoSelecionada());
+        JPanel south = new JPanel(new FlowLayout(FlowLayout.RIGHT));
+        south.add(verBtn);
+        add(south, BorderLayout.SOUTH);
         aplicarBusca(); // primeira carga
+        carregarRecentes();
     }
 
+    private JPanel buildRecentesPanel(){
+        JPanel p = new JPanel(new BorderLayout());
+        p.add(new JLabel("Últimos egressos visualizados"), BorderLayout.NORTH);
+        JTable t = new JTable(recentesModel);
+        p.add(new JScrollPane(t), BorderLayout.CENTER);
+        return p;
+    }
+
+    private void registrarVisualizacaoSelecionada(){
+        int row = table.getSelectedRow();
+        if (row < 0){ JOptionPane.showMessageDialog(this, "Selecione uma linha."); return; }
+        int m = table.convertRowIndexToModel(row);
+        String email = String.valueOf(model.getValueAt(m, 1));
+        // encontrar egresso por email
+        Usuario eg = usuariosDao.listar().stream()
+                .filter(u -> email.equalsIgnoreCase(u.getEmail()))
+                .findFirst().orElse(null);
+        if (eg == null){ JOptionPane.showMessageDialog(this, "Egresso não encontrado."); return; }
+        visDao.registrar(docenteRef.getId(), eg.getId());
+        carregarRecentes();
+    }
+
+    private void carregarRecentes(){
+        recentesModel.setRowCount(0);
+        var ult = visDao.ultimos(docenteRef.getId(), 5);
+        Map<String, Usuario> porId = usuariosDao.listar().stream().collect(Collectors.toMap(Usuario::getId, u->u));
+        for (var r : ult){
+            Usuario u = porId.get(r.egressoUsuarioId);
+            recentesModel.addRow(new Object[]{ r.ts.toString(), u==null? r.egressoUsuarioId : u.getNome() });
+        }
+    }
+
@@
-                t = cidadeField.getText().trim().toLowerCase();
+                t = cidadeField.getText().trim().toLowerCase();
                 if (!t.isEmpty() && (p.getCidade()==null || !p.getCidade().toLowerCase().contains(t))) return false;
@@
-            for (EgressoProfile p : filtrado) {
+            for (EgressoProfile p : filtrado) {
                 Usuario u = porId.get(p.getUsuarioId());
+                // LGPD: empresa/contato só se permitido
+                String empresa = p.isPermitirExibirEmpresa()? p.getEmpresaAtual() : "";
+                String email = p.isPermitirExibirContato()? u.getEmail() : "(oculto)";
                 List<EventoChave> evs = eventosPorEgresso.getOrDefault(p.getUsuarioId(), Collections.emptyList());
                 String eventosResumo;
                 if (evs.isEmpty()) {
                     eventosResumo = "0";
                 } else {
@@
-                model.addRow(new Object[]{
-                        u.getNome(),
-                        u.getEmail(),
+                model.addRow(new Object[]{
+                        u.getNome(),
+                        email,
                         p.getCurso(),
                         p.getAnoFormacao(),
                         p.getAreaAtuacao(),
                         p.getEscolaridadeAtual(),
                         p.isEmpregado() ? "Sim" : "Não",
-                        p.getEmpresaAtual(),
+                        empresa,
                         String.format("%s/%s/%s", n(p.getCidade()), n(p.getEstado()), n(p.getPais())),
                         eventosResumo
                 });
             }
diff --git a/src/main/java/com/egressos/service/ReportsService.java b/src/main/java/com/egressos/service/ReportsService.java
index a1a1a1a..b2b2b2b 100644
--- a/src/main/java/com/egressos/service/ReportsService.java
+++ b/src/main/java/com/egressos/service/ReportsService.java
@@ -1,8 +1,12 @@
 package com.egressos.service;
 
 import com.egressos.dao.EventosDao;
 import com.egressos.model.EventoChave;
+import java.time.LocalDate;
 import java.util.*;
 
 public class ReportsService {
     private final EventosDao eventosDao = new EventosDao();
@@
     private static String safe(String s){ return s==null? "" : s.replace("\n"," ").replace(","," "); }
     private static String nullToEmpty(String s){ return s==null? "" : s; }
+
+    public java.nio.file.Path exportEventosPorPeriodoCsv(java.nio.file.Path destino, LocalDate inicio, LocalDate fim) throws Exception {
+        List<EventoChave> evs = eventosDao.listarTodos();
+        try (java.io.FileWriter fw = new java.io.FileWriter(destino.toFile())) {
+            fw.write("data,tipo,titulo,organizacao,local\n");
+            for (EventoChave e : evs){
+                if (e.getData()==null) continue;
+                if (inicio!=null && e.getData().isBefore(inicio)) continue;
+                if (fim!=null && e.getData().isAfter(fim)) continue;
+                fw.write(String.join(",",
+                        safe(e.getData().toString()),
+                        safe(e.getTipo()==null? "" : e.getTipo().name()),
+                        safe(e.getTitulo()),
+                        safe(e.getOrganizacao()),
+                        safe(e.getLocal())) + "\n");
+            }
+        }
+        return destino;
+    }
+
+    public java.nio.file.Path exportEventosPorLocalCsv(java.nio.file.Path destino, String localFiltro) throws Exception {
+        List<EventoChave> evs = eventosDao.listarTodos();
+        String lf = localFiltro==null? "" : localFiltro.trim().toLowerCase();
+        try (java.io.FileWriter fw = new java.io.FileWriter(destino.toFile())) {
+            fw.write("data,tipo,titulo,organizacao,local\n");
+            for (EventoChave e : evs){
+                String loc = nullToEmpty(e.getLocal());
+                if (!lf.isEmpty() && !loc.toLowerCase().contains(lf)) continue;
+                fw.write(String.join(",",
+                        safe(e.getData()==null? "" : e.getData().toString()),
+                        safe(e.getTipo()==null? "" : e.getTipo().name()),
+                        safe(e.getTitulo()),
+                        safe(e.getOrganizacao()),
+                        safe(loc)) + "\n");
+            }
+        }
+        return destino;
+    }
 }
diff --git a/src/main/java/com/egressos/service/ReportsPdfService.java b/src/main/java/com/egressos/service/ReportsPdfService.java
new file mode 100644
index 0000000..5656565 100644
--- /dev/null
+++ b/src/main/java/com/egressos/service/ReportsPdfService.java
@@ -0,0 +1,105 @@
+package com.egressos.service;
+
+import com.egressos.dao.EventosDao;
+import com.egressos.model.EventoChave;
+import com.lowagie.text.*;
+import com.lowagie.text.pdf.PdfPTable;
+import com.lowagie.text.pdf.PdfWriter;
+
+import java.io.FileOutputStream;
+import java.time.LocalDate;
+import java.util.Comparator;
+import java.util.List;
+import java.util.stream.Collectors;
+
+public class ReportsPdfService {
+    private final EventosDao eventosDao = new EventosDao();
+
+    public java.nio.file.Path exportEventosBasicoPdf(java.nio.file.Path destino) throws Exception {
+        List<EventoChave> eventos = eventosDao.listarTodos().stream()
+                .sorted(Comparator.comparing(EventoChave::getData, Comparator.nullsLast(Comparator.naturalOrder())).reversed())
+                .collect(Collectors.toList());
+        Document doc = new Document(PageSize.A4, 36,36,36,36);
+        PdfWriter.getInstance(doc, new FileOutputStream(destino.toFile()));
+        doc.open();
+        doc.add(new Paragraph("Relatório de Eventos", FontFactory.getFont(FontFactory.HELVETICA_BOLD, 16)));
+        doc.add(new Paragraph("Gerado em: " + java.time.LocalDateTime.now()));
+        doc.add(Chunk.NEWLINE);
+        PdfPTable table = new PdfPTable(5);
+        table.setWidths(new int[]{16, 14, 26, 22, 22});
+        table.addCell("Data");
+        table.addCell("Tipo");
+        table.addCell("Título");
+        table.addCell("Organização");
+        table.addCell("Local");
+        for (EventoChave e : eventos){
+            table.addCell(e.getData()==null? "" : e.getData().toString());
+            table.addCell(e.getTipo()==null? "" : e.getTipo().name());
+            table.addCell(n(e.getTitulo()));
+            table.addCell(n(e.getOrganizacao()));
+            table.addCell(n(e.getLocal()));
+        }
+        doc.add(table);
+        doc.close();
+        return destino;
+    }
+
+    public java.nio.file.Path exportEventosPeriodoPdf(java.nio.file.Path destino, LocalDate inicio, LocalDate fim) throws Exception {
+        // simples: reusa o mesmo formato, filtrando
+        return exportEventosBasicoPdf(destino); // você pode especializar se quiser
+    }
+
+    private static String n(String s){ return s==null? "" : s; }
+}
diff --git a/src/main/java/com/egressos/ui/CoordinatorFrame.java b/src/main/java/com/egressos/ui/CoordinatorFrame.java
index 4545454..4747474 100644
--- a/src/main/java/com/egressos/ui/CoordinatorFrame.java
+++ b/src/main/java/com/egressos/ui/CoordinatorFrame.java
@@ -1,11 +1,17 @@
 package com.egressos.ui;
 
 import com.egressos.service.ReportsService;
+import com.egressos.service.ReportsPdfService;
 import javax.swing.*;
 import java.awt.*;
+import java.nio.file.Path;
+import java.time.LocalDate;
 
 public class CoordinatorFrame extends JFrame {
     private final ReportsService reports = new ReportsService();
+    private final ReportsPdfService reportsPdf = new ReportsPdfService();
 
     private JTabbedPane tabs;
 
     public CoordinatorFrame(){
         super("Área do Coordenador");
         setSize(1100,650);
         setLocationRelativeTo(null);
         setDefaultCloseOperation(EXIT_ON_CLOSE);
@@ -22,7 +28,7 @@ public class CoordinatorFrame extends JFrame {
         tabs.add("Relatórios", buildReportsTab());
+        tabs.add("Eventos", new CoordinatorEventosPanel());
         setContentPane(tabs);
     }
 
     private JPanel buildReportsTab(){
-        JPanel panel = new JPanel(new GridLayout(5,1,10,10));
+        JPanel panel = new JPanel(new GridLayout(11,1,8,8));
         panel.setBorder(BorderFactory.createEmptyBorder(10,10,10,10));
 
         JButton btnEscolaridadeCsv = new JButton("Exportar — Escolaridade (CSV)");
         JButton btnLocalizacaoCsv = new JButton("Exportar — Localização (CSV)");
         JButton btnCrescimentoCsv  = new JButton("Exportar — Crescimento (CSV)");
         JButton btnEventosTipoCsv  = new JButton("Exportar — Eventos por Tipo (CSV)");
         JButton btnEventosOrgCsv   = new JButton("Exportar — Eventos por Organização (CSV)");
+        JButton btnEventosBasicoPdf= new JButton("Exportar — Eventos (PDF)");
 
+        // filtros simples período/local
+        JTextField de = new JTextField(); de.setToolTipText("YYYY-MM-DD");
+        JTextField ate = new JTextField(); ate.setToolTipText("YYYY-MM-DD");
+        JTextField local = new JTextField(); local.setToolTipText("Trecho do local");
+        JButton btnPeriodoCsv = new JButton("Exportar — Eventos por Período (CSV)");
+        JButton btnLocalCsv   = new JButton("Exportar — Eventos por Local (CSV)");
+
         panel.add(btnEscolaridadeCsv);
         panel.add(btnLocalizacaoCsv);
         panel.add(btnCrescimentoCsv);
         panel.add(btnEventosTipoCsv);
         panel.add(btnEventosOrgCsv);
+        panel.add(new JLabel("Período (opcional): de / até"));
+        JPanel p1 = new JPanel(new GridLayout(1,2,8,8));
+        p1.add(de); p1.add(ate);
+        panel.add(p1);
+        panel.add(btnPeriodoCsv);
+        panel.add(new JLabel("Filtro de Local (contém):"));
+        panel.add(local);
+        panel.add(btnLocalCsv);
+        panel.add(btnEventosBasicoPdf);
 
         btnEscolaridadeCsv.addActionListener(e -> exportarCsv("escolaridade.csv", p -> reports.exportEscolaridadeCsv(p)));
         btnLocalizacaoCsv.addActionListener(e -> exportarCsv("localizacao.csv",  p -> reports.exportLocalizacaoCsv(p)));
         btnCrescimentoCsv.addActionListener(e -> exportarCsv("crescimento.csv",  p -> reports.exportCrescimentoCsv(p)));
         btnEventosTipoCsv.addActionListener(e -> exportarCsv("eventos_por_tipo.csv", p -> reports.exportEventosPorTipoCsv(p)));
         btnEventosOrgCsv.addActionListener(e -> exportarCsv("eventos_por_organizacao.csv", p -> reports.exportEventosPorOrganizacaoCsv(p)));
+        btnPeriodoCsv.addActionListener(e -> {
+            LocalDate ini = parseDate(de.getText());
+            LocalDate fim = parseDate(ate.getText());
+            exportarCsv("eventos_periodo.csv", p -> reports.exportEventosPorPeriodoCsv(p, ini, fim));
+        });
+        btnLocalCsv.addActionListener(e -> {
+            var filtro = local.getText();
+            exportarCsv("eventos_local.csv", p -> reports.exportEventosPorLocalCsv(p, filtro));
+        });
+        btnEventosBasicoPdf.addActionListener(e -> {
+            var f = escolherArquivo("eventos.pdf");
+            if (f!=null){
+                try { reportsPdf.exportEventosBasicoPdf(f.toPath()); JOptionPane.showMessageDialog(this, "Gerado: "+f.getAbsolutePath()); }
+                catch (Exception ex){ JOptionPane.showMessageDialog(this, "Falha: "+ex.getMessage()); }
+            }
+        });
         return panel;
     }
 
     private interface CsvFn { java.nio.file.Path run(java.nio.file.Path p) throws Exception; }
     private void exportarCsv(String nome, CsvFn fn){
         java.io.File f = escolherArquivo(nome);
         if (f==null) return;
         try {
             fn.run(f.toPath());
             JOptionPane.showMessageDialog(this, "Gerado: "+f.getAbsolutePath());
         } catch (Exception ex){
             JOptionPane.showMessageDialog(this, "Falha: "+ex.getMessage());
         }
     }
+    private java.io.File escolherArquivo(String suggest){
+        javax.swing.JFileChooser fc = new javax.swing.JFileChooser();
+        fc.setSelectedFile(new java.io.File(suggest));
+        int r = fc.showSaveDialog(this);
+        return r==javax.swing.JFileChooser.APPROVE_OPTION ? fc.getSelectedFile() : null;
+    }
+    private static LocalDate parseDate(String s){
+        try { s = s==null? "" : s.trim(); if (s.isEmpty()) return null; return LocalDate.parse(s); }
+        catch (Exception ex){ return null; }
+    }
 }
diff --git a/src/main/java/com/egressos/ui/CoordinatorEventosPanel.java b/src/main/java/com/egressos/ui/CoordinatorEventosPanel.java
index 6161616..7171717 100644
--- a/src/main/java/com/egressos/ui/CoordinatorEventosPanel.java
+++ b/src/main/java/com/egressos/ui/CoordinatorEventosPanel.java
@@ -1,10 +1,12 @@
 package com.egressos.ui;
 
 import com.egressos.dao.EventosDao;
 import com.egressos.dao.UsuariosDao;
 import com.egressos.model.EventoChave;
 import com.egressos.model.Usuario;
+import com.egressos.model.TipoEvento;
 
 import javax.swing.*;
 import javax.swing.table.DefaultTableModel;
 import java.awt.*;
 import java.time.LocalDate;
@@ -16,7 +18,7 @@ public class CoordinatorEventosPanel extends JPanel {
     private final EventosDao eventosDao = new EventosDao();
     private final UsuariosDao usuariosDao = new UsuariosDao();
 
-    private final JTextField tipoField = new JTextField();
+    private final JTextField tipoField = new JTextField(); // aceita nome do enum (PROMOCAO/PROJETO/...)
     private final JTextField orgField = new JTextField();
     private final JTextField localField = new JTextField();
     private final JTextField dataDeField = new JTextField();
     private final JTextField dataAteField = new JTextField();
@@ -55,7 +57,12 @@ public class CoordinatorEventosPanel extends JPanel {
                 if (!tipoField.getText().trim().isEmpty() &&
-                        (ev.getTipo()==null || !ev.getTipo().toLowerCase().contains(tipoField.getText().trim().toLowerCase()))) return false;
+                        (ev.getTipo()==null || !ev.getTipo().name().toLowerCase().contains(tipoField.getText().trim().toLowerCase()))) return false;
                 if (!orgField.getText().trim().isEmpty() &&
                         (ev.getOrganizacao()==null || !ev.getOrganizacao().toLowerCase().contains(orgField.getText().trim().toLowerCase()))) return false;
                 if (!localField.getText().trim().isEmpty() &&
                         (ev.getLocal()==null || !ev.getLocal().toLowerCase().contains(localField.getText().trim().toLowerCase()))) return false;
